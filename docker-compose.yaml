version: '3'

services:
  server:
    image: strapi/strapi
    volumes:
      - ./server:/srv/app
    ports:
      - '1337:1337'
    depends_on:
      - db
  db:
    image: postgres
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
